Jrcmtctl2 については、web-gui内のsrc フォルダのファイルを添付しております。

他の一点は、APIのコードです。
この実行には、下記のパッケージが必要です。
axios  ("npm install axios --save")
moment ("npm install moment --save"）
crypt (npm install crypto --save)

テストは、ルートのフォルダからターミナルのコマンドラインで、
npm install を走らせてから、

node .\create_delete_dashboard.js

として実行してください。

よろしくお願いいたします。

